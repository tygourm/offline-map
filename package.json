{
  "name": "offline-map",
  "version": "0.0.0",
  "scripts": {
    "lint": "prettier -c . && cd frontend && pnpm eslint",
    "format": "prettier -w ."
  },
  "packageManager": "pnpm@10.15.0",
  "devDependencies": {
    "@trivago/prettier-plugin-sort-imports": "^5.2.2",
    "prettier": "^3.6.2"
  }
}
