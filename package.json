{
  "name": "offline-map",
  "version": "1.0.0",
  "scripts": {
    "lint": "prettier -c . && ruff check . --no-cache && cd frontend && pnpm eslint",
    "format": "prettier -w . && ruff format . --no-cache"
  },
  "packageManager": "pnpm@10.15.0",
  "devDependencies": {
    "@trivago/prettier-plugin-sort-imports": "^5.2.2",
    "prettier": "^3.6.2"
  }
}
